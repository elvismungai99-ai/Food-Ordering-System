spring:
  application:
    name: backend
  devtools:
    restart:
      enabled: false
  datasource:
    url: jdbc:postgresql://localhost:5432/Food_Ordering_db
    username: food_user
    password: foodie123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080

jwt:
  secret: f00d0rd3r1ng$yst3mS3cr3tK3y2026!!SecureKey32Chars

app:
  frontend-base-url: http://localhost:5173
  mail:
    from: no-reply@foodordering.local

openrouteservice:
  base-url: https://api.openrouteservice.org
  api-key: ${ORS_API_KEY:}

security:
  rate-limit:
    ignore-localhost: false
